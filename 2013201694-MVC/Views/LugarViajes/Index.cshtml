@model IEnumerable<_2013201694_ENT.LugarViaje>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Servicio.NombreServicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreLugar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Servicio.NombreServicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreLugar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LugarViajeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LugarViajeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LugarViajeId })
        </td>
    </tr>
}

</table>
