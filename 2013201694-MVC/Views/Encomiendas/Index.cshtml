@model IEnumerable<_2013201694_ENT.Encomienda>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Venta.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreServicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tarifa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AsuntoEncomienda)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreDestinatario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Venta.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreServicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tarifa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AsuntoEncomienda)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreDestinatario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServicioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServicioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServicioId })
        </td>
    </tr>
}

</table>
