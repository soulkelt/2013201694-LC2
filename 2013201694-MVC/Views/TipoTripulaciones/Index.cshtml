@model IEnumerable<_2013201694_ENT.TipoTripulacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tripulacion.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tripulacion.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoTripulacionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoTripulacionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoTripulacionId })
        </td>
    </tr>
}

</table>
